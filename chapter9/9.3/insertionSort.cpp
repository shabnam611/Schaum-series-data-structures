#include<stdio.h>
#include<stdlib.h>
int INSERTCOUNT(int A[] , int N , int NUMB){
    int maxi = 0 ;
    int k,temp,l,d,c;
    for(l=0;l<N;l++){

    }

    for(k=1;k<=(N-1);k++){
        printf("%d", A[k]);
    }
}
int main(){
    int i,j,com = 0;
    scanf("%d", &i);
    int arr[i];
    for(j=1;j<=(i-1);j++){
        scanf("%d", &arr[j]);
    }
    INSERTCOUNT(arr,i,com);
    return 0;
}
