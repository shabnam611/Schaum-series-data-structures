#include<stdio.h>
#include<math.h>

int BINARY(int ARRAY[], int LB, int UB , int ITEM, int LOC){

}

int main(){
    int i,j,k,n;
    scanf("%d", &n);
    int arr[n];
    for(i=0;i<n;i++){
        scanf("%d", &arr[i]);
    }
    BINARY(arr,j,n,k,l)
    return 0;
}
